# @package _global_

defaults:
  - base
  - /preset/datasets/db_1280_aug
  - /preset/models/model_v4
  - /preset/lightning_modules/base
  - _self_

trainer:
  max_epochs: 20

dataloaders:
  train_dataloader:
    batch_size: 2
    shuffle: True
    num_workers: 4
  val_dataloader:
    batch_size: 2
    shuffle: False
    num_workers: 4
  test_dataloader:
    batch_size: 2
    shuffle: False
    num_workers: 4
